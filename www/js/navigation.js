/*
  navigation.js
  -------------

  Copyright (c) 2018, Reactane, all rights reserved.

*/

( this.undefine || require( 'undefine' )( module, require ) )()
( 'navigation', [ [ 'rs', 'toubkal' ] ], function( rs ) {
  "use strict";
  
  return rs
    
    /* ---------------------------------------------------------------------------------------------------------------------
      @pipelet $navigation( $selector, options )
      
      @description:
      
      @source:
      
      @parameters
      - $selector (String/Pipelet):
      - options           (Object): optionnal object

    */
    .Compose( '$navigation', function( source, $selector, options ) {
    } ) // $navigation()
  ;
} ); // module.export
